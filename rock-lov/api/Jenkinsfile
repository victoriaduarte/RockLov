pipeline {
    agent {
        docker {
            image 'ruby'
            args '--network rocklov-network'
        }
    }

    stages {
        stage('Prep') {
            steps {
                sh 'cd rock-lov/api && bundle install'
            }
        }
        stage('Testing') {
            steps {
                sh 'cd rock-lov/api && rspec'
            }
        }
    }
}